<Window x:Class="FireBirdConfigTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="FireBird Config Tool"
        Icon="assets/icon.ico"
        Height="850" Width="1150"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="#1B1D21">
    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#E8E8E8" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2A2D33" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseTitleBarButtonStyle" BasedOn="{StaticResource TitleBarButtonStyle}" TargetType="Button">
            <Setter Property="FontSize" Value="22" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C42B2B" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="42"
                            ResizeBorderThickness="8"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="42" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom dark title bar -->
        <Grid Grid.Row="0" Background="#17191D" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
              shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="46" />
                <ColumnDefinition Width="46" />
                <ColumnDefinition Width="46" />
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0" Source="assets/icon.ico" Width="22" Height="22" Margin="12,0,10,0" />
            <TextBlock Grid.Column="1"
                       Text="FireBird Config Tool"
                       VerticalAlignment="Center"
                       Foreground="#F2F2F2"
                       FontSize="14"
                       FontWeight="SemiBold" />

            <Button Grid.Column="2" Content="—" Click="MinimizeButton_Click"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Style="{StaticResource TitleBarButtonStyle}" />
            <Button Grid.Column="3" Content="□" Click="MaximizeButton_Click"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Style="{StaticResource TitleBarButtonStyle}" />
            <Button Grid.Column="4" Content="×" Click="CloseButton_Click"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Style="{StaticResource CloseTitleBarButtonStyle}" />
        </Grid>

        <Grid Grid.Row="1">
            <!-- Loading indicator shown until the config tool page finishes its own load -->
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Panel.ZIndex="0">
                <Image Source="assets/fb_icon.png" Width="96" Height="96" Margin="0,0,0,16"
                       Stretch="Uniform" SnapsToDevicePixels="True"
                       UseLayoutRounding="True" RenderOptions.BitmapScalingMode="HighQuality" />
                <TextBlock x:Name="StatusText"
                           Text="Starting FireBird Config Tool..."
                           HorizontalAlignment="Center"
                           FontSize="16"
                           Foreground="#666666" />
            </StackPanel>

            <wv2:WebView2 x:Name="webView"
                          Panel.ZIndex="1" />
        </Grid>
    </Grid>


</Window>
